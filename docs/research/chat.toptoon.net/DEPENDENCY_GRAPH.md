# Taiwan dependency graph

`/api/characters?limit=100` -> extraction script -> `data/characters.json` + `assets/tw/*` -> slim `data/characters.js` -> market grouping/search -> table/cards -> full-art dialog -> official detail link.

Stable identity is `character_id`; localized names and work labels are per-market presentation fields.
