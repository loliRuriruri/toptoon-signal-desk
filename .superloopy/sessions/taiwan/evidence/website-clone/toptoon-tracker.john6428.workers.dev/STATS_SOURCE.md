# Statistics source contract

STATUS: PASS WITH CAVEATS

The reference main screen is a dark panel dashboard backed by public JSON endpoints. The local reproduction snapshots the following endpoints on 2026-08-24: revenue nowcast, revenue by character, coin mix ramp, completion ceiling, growth/cannibalization, monthly index, totals time series, daily totals, daily chat totals, characters, site comparison, site traction, and site revenue.

The UI must retain the source caveats: revenue run-rate and profit are modeled estimates, the IR benchmark and margin are company claims, and short collection windows are extrapolations. The reproduction is a dated snapshot, not a live financial statement.
